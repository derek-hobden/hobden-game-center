import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Standard Next.js App Router config — deploy as-is on Vercel.
  // Service worker and manifest live under /public for HTTPS production.
};

export default nextConfig;
